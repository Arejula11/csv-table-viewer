{
  "name": "csv-table-viewer",
  "displayName": "CSV Table Viewer",
  "description": "Display CSV files as clean, interactive tables inside VS Code, supporting filtering and smart sorting for numbers, text, and European dates.",
  "version": "1.0.0",
  "publisher": "Arejula11",
  "icon": "public/assets/favicon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/Arejula11/csv-table-viewer.git"
  },
  "bugs": {
    "url": "https://github.com/Arejula11/csv-table-viewer/issues"
  },
  "homepage": "https://github.com/Arejula11/csv-table-viewer#readme",
  "license": "MIT",
  "keywords": [
    "csv",
    "table",
    "viewer",
    "data",
    "sort",
    "filter",
    "spreadsheet"
  ],
  "engines": {
    "vscode": "^1.105.1"
  },
  "categories": [
    "Data Science",
    "Other"
  ],
  "activationEvents": [
    "onLanguage:csv"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "csv-viewer.openAsTable",
        "title": "Open CSV as Table",
        "icon": {
          "light": "codicon-table",
          "dark": "codicon-table"
        }
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.19.0",
    "@types/vscode": "^1.105.0",
    "@typescript-eslint/eslint-plugin": "^8.45.0",
    "@typescript-eslint/parser": "^8.45.0",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.25.10",
    "eslint": "^9.36.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3"
  }
}
